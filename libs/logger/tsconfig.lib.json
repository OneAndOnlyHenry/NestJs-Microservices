{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "dist",
    "declaration": true,  // Generate .d.ts files
    "emitDeclarationOnly": false,  // Ensure JS files are emitted
    "noEmit": false  // Ensure compilation emits files
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}